'use client';

import LiveEventPage from '../live/page';

/** Player scoring. Admin scoring is /event/[id]/manage/scoring. */
export default function EventScoringPage() {
  return <LiveEventPage />;
}
